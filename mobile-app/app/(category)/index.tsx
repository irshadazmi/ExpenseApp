import { View, Text } from "react-native";
import React from "react";
import styles from "@/styles/styles";

const Catgories = () => {
	return (
		<View style={styles.container}>
			<Text style={styles.title}>Catgories</Text>
		</View>
	);
};

export default Catgories;
